const nomes = ['jose', 'joao', 'maria', 'joana', 'andre'];

// essa forma de construir o codigo é muito prática porém caso voce precise do indice
// é necessário usar outra formula
for (let alunos of nomes) {
    console.log(alunos);
}